{
  "note": "What the reference web server does when it runs (IMG-30). The smoke suite compares the running image with this, and a platform's runtime monitor can use it as the expected profile (PLT-16).",
  "processes": [
    "nginx"
  ],
  "listeners": [
    {
      "port": 8080,
      "protocol": "tcp",
      "purpose": "HTTP"
    },
    {
      "port": 8443,
      "protocol": "tcp",
      "purpose": "HTTPS, only when a TLS server block, certificate, and key are mounted"
    }
  ],
  "writable_paths": [
    "/tmp"
  ],
  "outbound": [],
  "mounts": [
    {
      "path": "/tmp",
      "type": "tmpfs",
      "options": "rw,noexec,nosuid,nodev",
      "required": true
    },
    {
      "path": "/etc/nginx/conf.d",
      "type": "read-only configuration",
      "required": false
    },
    {
      "path": "/etc/nginx/tls",
      "type": "read-only secret: server certificate and key",
      "required": false
    }
  ]
}
